name = "mctl-landing-form"
main = "index.js"
compatibility_date = "2024-01-01"

# Environment variables (set via Cloudflare dashboard or wrangler secret)
# TELEGRAM_BOT_TOKEN = "your-bot-token-here"
# TELEGRAM_CHAT_ID = "your-chat-id-here"

# Route configuration (optional, or use default workers.dev subdomain)
# routes = [
#   { pattern = "platform.mctl.me/api/submit", zone_name = "mctl.me" }
# ]
